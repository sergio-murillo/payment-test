# Configuración para despliegue a S3 + CloudFront
# Copia este archivo como .env.deploy y configura los valores

# Región de AWS donde están los recursos
AWS_REGION=us-east-1

# Nombre del bucket de S3 donde se desplegará el frontend
S3_BUCKET_NAME=wompi-payments-frontend

# ID de la distribución de CloudFront
CLOUDFRONT_DISTRIBUTION_ID=E1234567890ABC

# URL del backend API (debe ser la URL de producción)
NEXT_PUBLIC_API_URL=https://api.wompi-payments.com

# Perfil de AWS a usar (opcional, por defecto: default)
AWS_PROFILE=default

# Directorio de salida del build (opcional, por defecto: out)
BUILD_OUTPUT_DIR=out

# Rutas a invalidar en CloudFront (opcional, por defecto: /*)
INVALIDATE_PATHS=/*
